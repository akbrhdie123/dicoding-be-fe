const hitungkelpersegipanjang = (panjang, lebar) => {
    return 2 * (panjang + lebar);
}

const panjang = 52;
const lebar = 15;

const keliling = hitungkelpersegipanjang(panjang, lebar);

console.log(keliling);